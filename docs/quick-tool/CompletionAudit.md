# Quick-tool contract completion audit

**Verdict: the selected investigation contract is satisfied.** Confidence is high for the documented policies, source traceability, installed signatures, and isolated assignment boundary. This is not a verdict that the feature, production inventory adapter, or multiplayer behavior is implemented or validated. No required investigation item remains failed or materially unverified.

Independent review performed on 2026-09-23 using the audit-stage-completion workflow. The reviewer read the controlling documents directly, inspected the working diff and retained probe, checked relevant source definitions, and independently reran the isolated installed-assembly probe. The implementer's SecondReview.md was considered supporting evidence, not accepted as proof by itself. No production files or checklist markers were changed by this audit.

## Controlling documentation and scope

The controlling selection is `QuickToolRadialMenu.todo`, **Phase 1: Resolve policy and prove inventory integration contracts**, including its nested authority tasks, completion criteria, inherited Goal and scope, Source evidence and implementation constraints, and D1-D7. Its source link resolves from the repository root to [QuickToolRadialMenu.proposal.md](../../QuickToolRadialMenu.proposal.md), which was read in full. User decisions on worn-first durability, deterministic positions, cached mesh rendering, and exclusion of performance testing remain controlling.

The plan's implementation-contract and evidence links resolve to [ImplementationContract.md](ImplementationContract.md) and [InventoryContractEvidence.md](InventoryContractEvidence.md). Their nested [probe](VerifyInstalledInventoryContract.ps1), [raw output](installed-api-probe.txt), and [second review](SecondReview.md) were inspected directly. The contract's `../../` repository paths and `../../../vsapi/` source paths resolve correctly from this directory. Source shorthand was interpreted under its stated root conventions, not from the shell working directory. In particular, the identity reference to ItemStack resolves to `vsapi/Common/Collectible/ItemStack.cs`; ItemSlot resolves to `vsapi/Common/Inventory/ItemSlot.cs`. Directory-level GUI/rendering references were examined through relevant dialog, renderer, and resource owners, not treated as a requirement to read unrelated files.

Repository revision: `d08f491a169a0cd3a102544dea399554ee1705cb`. Adjacent API revision: `63d33f70018c606fb0f70ead900fe90625fbd7b2`. Both were independently confirmed. Installed API/library versions are 1.22.7.0; project references use VINTAGE_STORY. The observed changes are planning/evidence documents and a retained investigation script. Production rendering or equipment code is intentionally absent.

## Task and requirement mapping

| Contract item | Status | Applicable source and independently checked evidence |
| --- | --- | --- |
| D1 ranking and unsupported metadata | Satisfied | Proposal Candidate Discovery and Cache; contract Resolved decisions. Highest tier, lowest positive absolute remaining durability, then hotbar/backpack and slot order are explicit. Non-wearing tools rank at infinity within tier; broken and unsupported metadata are excluded. Installed GetTool, GetToolTier, GetMaxDurability, and GetRemainingDurability signatures match the chosen inputs. |
| D2 eligible inventories/categories | Satisfied | Proposal Candidate Discovery; contract Inventory observation and identity. Own hotbar ordinary slots and backpack content are selected; external/equipment/bag-equipping slots are excluded. Installed NewSlot IL distinguishes skill index 10 and offhand 11. Compatibility is bounded to verified inventory/slot contracts; unknown implementations do not receive unsafe writes. |
| D3 chained original-item restoration | Satisfied | Proposal Switching Between Quick-Tools; contract Movement tables. A remains the restoration target across B and C. Independent fixture execution restored every exact A/B/C reference to its original slot. |
| D4 manual hand changes | Satisfied | Proposal Inventory Changes During a Session; contract Input contract and server lifecycle. Client and server AfterActiveSlotChanged invalidate, including away-and-back history. Server SlotModified checks reference/quantity outside its internal publication guard; request-time checks supplement events. Installed server signatures and source IServerEventAPI around line 190 support the seam. |
| D5 return-slot fallback | Satisfied | Proposal Unequip; contract D5 and Movement tables. Deterministic compatible empty-slot fallback preserves unrelated items and rejects an impossible complete arrangement before assignment. Full/restricted inventory behavior is specified; production validator tests remain later work. |
| D6 center/already-held behavior | Satisfied | Proposal Temporary Equipment Session and external changes; contract D6. Invalid-session center is disabled. Already-held selection is a no-op without new history. General manual unequip is explicitly outside scope. |
| D7 exact stable layout | Satisfied | Proposal Interaction/Rendering; contract Canonical category mapping. All 33 installed category names have explicit indices, with wedge zero centered at screen-up and clockwise ordering. Missing tools retain wedges; unknown numeric categories cannot dynamically insert entries. Extension requires an explicit supported-set change while closed. |
| Registration, live configuration, player/world lifecycle, localization, GUI ownership | Satisfied | Proposal System Responsibilities and cleanup requirements; contract Functionality and lifecycle integration. Checked VanillaExpandedModSystem.Start channel registration, VanillaExpandedConfig, ILiveConfigurable, LiveConfigReload, ConfigLib integration, Constants, EquipLightSource, and GuiDialogAlloyCalculator conventions. Two independent owners and thin roots are retained; disable/re-enable and local/server teardown are explicit. |
| Press/hold/release/repeat/pointer/click/focus/active-slot contracts | Satisfied | Proposal Interaction; contract Input contract. Checked IInputAPI raw versus dialog-filtered state, IClientEventAPI low-level handled events, HotKey.CurrentMapping/TriggerOnUpAlso, KeyEvent/MouseEvent, and GuiDialog input methods. Empty-composer default mouse handling is insufficient, so raw capture and explicit handling are planned. A closed-until-release latch, matching mouse-up guard, and focus adapter cover identified seams. Live routing is not asserted from signatures. |
| Inventory enumeration/subscription, replacement and durability | Satisfied | Proposal Candidate Discovery; contract Inventory observation. Checked IPlayerInventoryManager own-inventory boundary and unordered Inventories warning, IInventory.SlotModified, InventoryBase.DidModifyItemSlot, BagInventory reload/save behavior, and CollectibleObject.DamageItem ending in MarkDirty. Topology reconciliation, replacement unsubscription, coalescing, open-time refresh, and dirty-notification compatibility requirements are explicit. No per-frame item scan is introduced. |
| Item identity | Satisfied | Proposal Temporary Equipment Session and Inventory Changes; contract identity section. Checked ItemStack identity/attributes, ItemSlot whole-stack operations, BagInventory replacement, and installed UpdateSlotStack setter call. Server reference/quantity continuity protects restoration; client object references and fingerprints do not masquerade as immutable identity. Duplicate-looking stale references reject in the fixture. |
| Client prediction, server validation, result acceptance/rejection, notifications | Satisfied | Proposal Authority and Failure Handling; contract Boundary and request lifecycle / Result and synchronization. Existing AlloyDepositSystem, packet types, and AlloyDepositService were inspected as patterns, not copied transaction proof. No client prediction is planned; authenticated server derivation, main-thread dispatch, serialization, sequence deduplication, EndSession generations, and result/native-sync reconciliation are explicit. |
| Intermediate A-B, B-C, restore, restricted/full states | Satisfied | Proposal restoration example; contract Movement tables plus alias/moved-A rules. Two-slot and three-slot cycles need no extra empty slot. Empty hand and original-A selection are specified. Reference-conserving full cycles and stale/precommit rejection passed the actual installed-slot fixture. Restricted/full eligibility remains a controlled fixture input, not a production acceptance claim. |
| Complete movement validation/commit without partial exposure | Satisfied | Proposal supported movement and no partial-result requirements; contract Commit and notification boundary. Installed Itemstack setter is nonvirtual plain assignment, IL 02037D251200042A. All final references are assigned synchronously before callbacks. Native OnItemSlotModified(previousStack) follows only after the complete permutation; fixture observers see the completed state. This is game-thread observer atomicity, not crash durability or rollback of arbitrary external code. |
| Need for authoritative adapter and exact boundary | Satisfied | Contract authority and installed evidence. Source TryFlipItems predicts one flip before obtaining its packet; installed network handling accepts/reverts individual flips. Repeated flips are not multi-slot atomicity. A narrow custom request/result adapter is therefore specified, using public native BroadcastPlayerData(true) and BroadcastHotbarSlot for publication, not protected SendInventoryContents methods. |
| Rejection/interruption/reconciliation guarantee | Satisfied | Proposal Interaction and Authority; contract commit/result sections. Before commit, rejection changes no slots. After commit, notification/send failure is CommittedNeedsReconcile, never unchanged rejection or destructive snapshot rollback. The fixture independently exercises postcommit callback failure. Status refresh, both result/update arrival orders, and disconnect behavior are defined for subsequent implementation tests. |
| Ownership map and focused validation cases | Satisfied | Proposal System Responsibilities, Rendering, Acceptance; contract Ownership map and Focused verification matrix. Checked radial-progress renderer/resources, test project references, README Testing, build.ps1, and CakeBuild tasks. Generic rendering stays independent of tool logic; cached combined geometry and shader state remain approved. Narrow slot seams, source-comment conventions, future correctness/live checks, and no performance gate are retained. |
| Completion: all decisions resolved | Satisfied | D1-D7 and proposal reconciliation are explicit before dependent consumers. No unresolved policy is silently treated as an installed capability. |
| Completion: installed input/event/identity/authority/completion evidence | Satisfied | Raw installed signatures/IL and evidence report identify the available seams and explicitly separate live behavior. Versions/hashes matched the independent rerun. |
| Completion: chained movement demonstration and retained useful evidence | Satisfied | Independent rerun of the reproducible fixture passed; script/log remain as focused investigation evidence. No disposable production bypass or broad build scaffolding was introduced. |

## Discrepancies challenged against the full contract

| Area | Classification | Evidence and disposition | Required action |
| --- | --- | --- | --- |
| Missing production menu/cache/adapter and tests | Expected or intended | The selected outcome is a concrete contract and safe movement design before consumers. Subsequent checklist sections explicitly own production implementation. | Keep subsequent implementation items unchecked. |
| Detached fixture does not prove restrictions or backpack persistence | Expected or intended; later evidence obligation | The probe explicitly supplies eligibility booleans and uses detached slots. Both evidence documents disclose this; installed native persistence call paths are separately inspected. This supports the primitive commit investigation, not a production validator claim. | Later verify actual CanHold/CanTake/capacity/topology checks and native bag persistence before claiming equipment completion. |
| Sequential assignments are not a general engine transaction | Expected or intended | Nonvirtual plain setter, main-thread serialization, and notification deferral establish the documented observer boundary. Arbitrary callback mutations and crashes are explicitly outside that guarantee. No claim of general transactional rollback remains. | Enforce the stated supported-adapter and no-callback commit constraints during implementation. |
| Postcommit notification failure cannot leave inventory unchanged | Resolved | Proposal now distinguishes rejection from committed-needs-reconciliation and matches the underlying API boundary. No misleading rollback claim remains. | Preserve these distinct outcomes in the eventual implementation and tests. |
| Request-time hand comparison misses away-and-back selection | Resolved | Final contract adds installed client/server after-change subscriptions and server held-slot notifications; locally ended generations also reach the server via EndSession. | Implement and test the recorded event-history contract later. |
| Focus/camera/click-through and native multiplayer ordering untested | Expected or intended; later evidence obligation | Installed seams are identified, but actual game routing and packet arrival are explicitly assigned to live acceptance. No game was launched or controlled by this audit. | Record actual live evidence during delivery validation, without treating this audit as a substitute. |
| Thirty-three wedges may be difficult to read | Expected or intended | D7 resolves the exact installed category set without inventory-dependent compaction. The contract explicitly requires later readability verification. | Evaluate visual correctness/readability during rendering acceptance; preserve deterministic disabled positions. |
| No performance/build/package run | Expected or intended | User excludes performance testing. No production implementation changed. Repository build/package behavior was inspected, including Package cleanup of Releases. An isolated probe is proportionate to this investigation. | No performance measurements required; run appropriate build/package checks when production artifacts exist. |

No confirmed issue, partially satisfied required item, or current-stage evidence gap remains after reconciliation. Later obligations above are retained because their execution is explicitly outside the selected contract; they are not waived.

## Independent verification result

Executed `pwsh -NoProfile -File docs/quick-tool/VerifyInstalledInventoryContract.ps1` as the delegated audit agent. Exit status was zero. Verified groups: unchanged precommit rejection/reference-conserving three-slot move/complete notification visibility/postcommit failure; A-B-A; A-B-C-A; stale identical-looking reference rejection; initially empty hand restoration. The probe's limitations were printed and retained.

API SHA256: `034283E7E9D98EAE45EE63005576FD89BADC3C995B531CC4C3FE46F3EB2D3296`.
Library SHA256: `E08F22B493B92FEAF0AAEB79D22437EA0F7EFC38AA7F72A04A47F98BC0E40DF0`.

`git diff --check` passed; Git emitted only its existing LF-to-CRLF conversion notices for the planning files. No performance test, broad feature build, package, deployment, or live-game operation was run. The parent may now update the selected completion markers and review-status text, while preserving all later implementation and live-validation obligations.
